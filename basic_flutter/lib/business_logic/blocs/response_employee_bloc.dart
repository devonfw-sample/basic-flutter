import 'package:flutter/material.dart';

class ResponseEmployeeBloc extends StatelessWidget {
  const ResponseEmployeeBloc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
